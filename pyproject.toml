[project]
name = "cashdash"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tom van Rees" }
]
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.3.0",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
]

[project.scripts]
cashdash = "cashdash:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
